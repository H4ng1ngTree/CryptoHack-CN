### Hint

- 根据上一篇背景阅读，如果两个点相加为 $O$，那么它们互为相反点。
- 对椭圆曲线上的点来说，相反点的 $x$ 坐标不变，$y$ 坐标取相反数。
- 所以先得到 $Q = (8045, -6936)$。不过题目是在有限域里做运算，还要把负数转成模 $9739$ 下的正数。

### 代码

```python
P = (8045, 6936)
p = 9739

x, y = P
Q = (x, (-y) % p)

print(Q)
```

### Result

```text
(8045, 2803)
```

### Flag

```text
crypto{8045,2803}
```