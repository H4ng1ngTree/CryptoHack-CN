# 点取负

在 starter set 的所有挑战中，我们都将使用如下椭圆曲线：

```text
E: Y^2 = X^3 + 497X + 1768 mod 9739
```

基于上述曲线，以及点 `P = (8045, 6936)`，请寻找一个点 `Q(x, y)`，使得：

```text
P + Q = O
```

其中 `O` 表示椭圆曲线加法群中的无穷远点。请注意，我们现在是在有限域中进行运算，因此需要正确处理负数的模运算。

在此输入 flag：`crypto{x,y}`
